# Database Configurations

This directory stores database-related assets, such as:

- Entity-Relationship Diagrams (ERDs)
- SQL migration scripts
- Docker Compose configurations for various databases (PostgreSQL, MongoDB, etc.)
- Seed data scripts
