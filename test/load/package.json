{
  "name": "load-test",
  "version": "1.0.0",
  "description": "Load and Performance Tests using k6",
  "scripts": {
    "test:local": "docker compose run --rm k6-local",
    "test:metrics": "docker compose run --rm k6-metrics"
  },
  "devDependencies": {
    "@types/k6": "^0.45.0",
    "typescript": "^5.2.2"
  }
}
